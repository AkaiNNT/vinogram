$('#myInput_<%=@post.id%>').val("");
if (<%=params[:comment][:comment_modal]%> == true) {

	$(".all_comments_<%=@post.id%>").append("<%= j render 'posts/comments/comment', comment: @comment, only_delete: true %>");
	$('.options-comment').addClass('d-none');
}
else $(".all_comments_<%=@post.id%>").append("<%= j render 'posts/comments/comment', comment: @comment, only_delete: false %>");
